# fibonacci.tie
# Calcula la secuencia de Fibonacci hasta el límite de 4 bits.
# Salida esperada: 0, 1, 1, 2, 3, 5, 8, 13

let a = 0
let b = 1
print a
print b

while b < 14:
    let c = a + b
    print c
    a = b
    b = c
